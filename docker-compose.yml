services:
  wake-sleep:
    build: .
    env_file: .env
    environment:
      - TZ=${TZ:-America/Sao_Paulo}
    volumes:
      - ./data:/app/data
      # Optional: enable Nginx Proxy Manager discovery by mounting its data
      # folder read-only and setting NPM_DISCOVERY_ENABLED=true.
      # - /path/to/nginx-proxy-manager/data:/npm-data:ro
      - /etc/localtime:/etc/localtime:ro
    restart: unless-stopped
    # Required for ICMP ping inside the container
    cap_add:
      - NET_RAW
    # Needed for Wake-on-LAN broadcast to reach your LAN.
    # On Linux this is the most reliable option.
    network_mode: "host"
    # If you can't use host networking (e.g. Docker Desktop), comment the line above
    # and use explicit port mapping instead:
    # ports:
    #   - "8080:8080"
    #   - "2222:2222"
    #   - "6790:6790"
    #   - "9000:9000"
    #   - "10000-10010:10000-10010"
